<Window x:Class="MG_Project.Desktop.Windows.AddEmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MG_Project.Desktop.ViewModels" 
        d:DataContext="{d:DesignInstance Type=vm:AddEmployeeViewModel}"
        mc:Ignorable="d"
        Title="Dodaj Pracownika" Height="550" Width="400"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Imię:" FontWeight="Bold"/>
                <TextBox Text="{Binding FirstName}" Margin="0,0,0,10" Padding="3"/>

                <TextBlock Text="Nazwisko:" FontWeight="Bold"/>
                <TextBox Text="{Binding LastName}" Margin="0,0,0,10" Padding="3"/>

                <TextBlock Text="PESEL:" FontWeight="Bold"/>
                <TextBox Text="{Binding Pesel}" Margin="0,0,0,10" Padding="3"/>

                <TextBlock Text="Stanowisko:" FontWeight="Bold"/>
                <TextBox Text="{Binding Position}" Margin="0,0,0,10" Padding="3"/>

                <TextBlock Text="Wynagrodzenie:" FontWeight="Bold"/>
                <TextBox Text="{Binding BaseSalary}" Margin="0,0,0,10" Padding="3"/>

                <Separator Margin="0,10"/>
                <TextBlock Text="Adres (Ulica / Nr domu / Kod / Miasto):" FontWeight="Bold"/>
                <TextBox Text="{Binding Street}" Margin="0,0,0,5" Padding="3" ToolTip="Ulica"/>
                <TextBox Text="{Binding HouseNumber}" Margin="0,0,0,5" Padding="3" ToolTip="Numer domu"/>
                <TextBox Text="{Binding ZipCode}" Margin="0,0,0,5" Padding="3" ToolTip="Kod pocztowy"/>
                <TextBox Text="{Binding City}" Margin="0,0,0,10" Padding="3" ToolTip="Miasto"/>

                <Separator Margin="0,10"/>
                <TextBlock Text="Firma:" FontWeight="Bold"/>
                <ComboBox ItemsSource="{Binding AvailableFirms}" 
                          SelectedItem="{Binding SelectedFirm}" 
                          DisplayMemberPath="Name"
                          Margin="0,0,0,10" Padding="5"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Anuluj" Command="{Binding CancelCommand}" Width="80" Margin="0,0,10,0" IsCancel="True"/>
            <Button Content="Zapisz" Command="{Binding SaveCommand}" Width="80" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>