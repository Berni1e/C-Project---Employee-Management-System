<Window x:Class="MG_Project.Desktop.Windows.EditFirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MG_Project.Desktop.ViewModels.Firms"
        d:DataContext="{d:DesignInstance Type=vm:EditFirmViewModel}"
        mc:Ignorable="d"
        Title="Edycja Firmy" 
        SizeToContent="WidthAndHeight" 
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Edytuj dane firmy" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Width="300">
            <TextBlock Text="Nazwa Firmy:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                     Padding="5" Margin="0,0,0,10"/>

            <TextBlock Text="Miasto (Siedziba):" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding City, UpdateSourceTrigger=PropertyChanged}" 
                     Padding="5" Margin="0,0,0,10"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Anuluj" 
                    Command="{Binding CancelCommand}" 
                    IsCancel="True"
                    Width="80" Margin="0,0,10,0" Padding="5"/>

            <Button Content="Zapisz" 
                    Command="{Binding SaveCommand}" 
                    IsDefault="True"
                    Width="80" Padding="5"/>
        </StackPanel>
    </Grid>
</Window>